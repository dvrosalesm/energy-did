# Attester CLI

<b>Important</b>
Build the project before running the commands

Available commands:

List pending claims to attest

```
npx . list-pending
```

Request verification

```
npx . attest <claim-id>
```
